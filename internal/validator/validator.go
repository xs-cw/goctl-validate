package validator

// 这个函数在plugin.go中已经定义，这里注释掉
/*
func ProcessPlugin(p *plugin.Plugin, options processor.Options) error {
	// ... 现有代码 ...
	
	// 解析API文件
	apiFile := p.ApiFilePath
	
	// 生成验证文件
	validationFile, err := GenerateValidationFile(apiFile, options)
	if err != nil {
		return err
	}
	
	// 输出结果
	fmt.Printf("成功生成验证文件: %s\n", validationFile)
	
	return nil
}
*/

// ... 现有代码 ... 